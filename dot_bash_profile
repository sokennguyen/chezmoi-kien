#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

if [[  -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then 
	exec startx
fi
alias vim="nvim"

# Created by `pipx` on 2025-02-08 21:25:54
export PATH="$PATH:/home/soken/.local/bin"

export GTK_IM_MODULE=ibus
export QT_IM_MODULE=ibus
export XMODIFIERS=@im=ibus
ibus-daemon -drx
